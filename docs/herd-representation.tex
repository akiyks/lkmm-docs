\section{Herd representation}
\label{sec:docs:Herd representation}

\begin{Note}
  Pretty-printed \path{tools/memory-model/Documentation/herd-representation.txt}.
\end{Note}

\begin{Note}
  Disclaimer.
  The table includes representations of \qco{add} and \qco{and} operations;
  corresponding/identical representations of \qco{sub}, \qco{inc}, \qco{dec},
  \qco{or}, \qco{xor}, and \qco{andnot} operations are omitted.
\end{Note}

\begin{Note}
  Note that the syntactic representation does not always match the sets and
  relations in \path{linux-kernel.cat}, due to redefinitions in
  \path{linux-kernel.bell} and \path{lock.cat}.
  For example, the \co{po}~link between~\co{LKR} and~\co{LKW} is upgraded
  to an \co{rmw}~link, and \co{W[ACQUIRE]} are not included in the
  \co{Acquire}~set.
\end{Note}

\begin{VerbatimU}[tabsize=8]
# Legend:
#	R,	a Load event
#	W,	a Store event
#	F,	a Fence event
#	LKR,	a Lock-Read event
#	LKW,	a Lock-Write event
#	UL,	an Unlock event
#	LF,	a Lock-Fail event
#	RL,	a Read-Locked event
#	RU,	a Read-Unlocked event
#	R*,	a Load event included in RMW
#	W*,	a Store event included in RMW
#	SRCU,	a Sleepable-Read-Copy-Update event
#
#	po,	a Program-Order link
#	rmw,	a Read-Modify-Write link - every rmw link is a po link
#
# By convention, a blank cell in a row means "same as the preceding row".
\end{VerbatimU}

\newcommand{\Dark}{\rowcolor{lightgray}}
\newcommand{\Extraspace}{\addlinespace[2pt]}

{\scriptsize
\centering
%\renewcommand*{\arraystretch}{1.1}
%\rowcolors{7}{lightgray}{}
%\ebresizewidth{
\begin{longtable}{rl}\toprule
	\multicolumn{2}{@{}l}{Non-RMW ops} \\
	C macro  &  Events \\
	\cmidrule(r){1-1} \cmidrule(l){2-2}
	\endfirsthead
	\multicolumn{2}{@{}l}{... continued} \\
	C macro  &  Events \\
	\cmidrule(r){1-1} \cmidrule(l){2-2}
	\endhead
	\midrule
	\multicolumn{2}{r@{}}{continued on next page ...} \\
	\endfoot
	\bottomrule
	\endlastfoot
\Dark	\tco{READ_ONCE}           & \tco{R[ONCE]} \\
\Dark	\tco{atomic_read}         & \\ \Extraspace
	\tco{WRITE_ONCE}          & \tco{W[ONCE]} \\
	\tco{atomic_set}          & \\ \Extraspace
\Dark	\tco{smp_load_acquire}    & \tco{R[ACQUIRE]} \\
\Dark	\tco{atomic_read_acquire} & \\  \Extraspace
	\tco{smp_store_release}   & \tco{W[RELEASE]} \\
	\tco{atomic_set_release}  & \\  \Extraspace
\Dark	\tco{smp_store_mb}        & \tco{W[ONCE] ->po F[MB]} \\
	\tco{smp_mb}              & \tco{F[MB]} \\
\Dark	\tco{smp_rmb}             & \tco{F[rmb]} \\
	\tco{smp_wmb}             & \tco{F[wmb]} \\
\Dark	\tco{smp_mb__before_atomic} & \tco{F[before-atomic]} \\
	\tco{smp_mb__after_atomic} & \tco{F[after-atomic]} \\
\Dark	\tco{spin_unlock}         & \tco{UL} \\ \Extraspace
	\tco{spin_is_locked}      & On seccess: \tco{RL} \\
	                          & On Failure: \tco{RU} \\ \Extraspace
\Dark	\tco{smp_mb__after_spinlock} & \tco{F[after-spinlock]} \\
	\tco{smp_mb__after_unlock_lock} & \tco{F[after-unlock-lock]} \\
\Dark	\tco{rcu_read_lock}       & \tco{F[rcu-lock]} \\
	\tco{rcu_read_unlock}     & \tco{F[rcu-unlock]} \\
\Dark	\tco{synchronize_rcu}     & \tco{F[sync-rcu]} \\
	\tco{rcu_dereference}     & \tco{R[ONCE]} \\
\Dark	\tco{rcu_assign_pointer}  & \tco{W[RELEASE]} \\ \Extraspace
	\tco{srcu_read_lock}      & \tco{R[srcu-lock]} \\
	\tco{srcu_down_read}      & \\ \Extraspace
\Dark	\tco{srcu_read_unlock}    & \tco{W[srcu-unlock]} \\
\Dark	\tco{srcu_up_read}        & \\ \Extraspace
	\tco{synchronize_srcu}    & \tco{SRCU[sync-srcu]} \\
\Dark	\tco{smp_mb__after_srcu_read_unlock} & \tco{F[after-srcu-read-unlock]} \\
	\midrule \Extraspace
	\multicolumn{2}{@{}l}{RMW ops w/o return value} \\
	C macro  &  Events \\
	\cmidrule(r){1-1} \cmidrule(l){2-2}
\Dark	\tco{atomic_add} & \tco{R*[NORETURN] ->rmw W*[NORETURN]} \\
\Dark	\tco{atomic_and} & \\ \Extraspace
	\tco{spin_lock}  & \tco{LKR ->po LKW} \\
	\midrule \Extraspace
	\multicolumn{2}{@{}l}{RMW ops w/ return value} \\
	C macro  &  Events \\
	\cmidrule(r){1-1} \cmidrule(l){2-2}
\Dark	\tco{atomic_add_return} & \tco{R*[MB] ->rmw W*[MB]} \\
\Dark	\tco{atomic_fetch_add} & \\
\Dark	\tco{atomic_fetch_and} & \\
\Dark	\tco{atomic_xchg} & \\
\Dark	\tco{xchg}        & \\
\Dark	\tco{atomic_add_negative} & \\ \Extraspace
	\tco{atomic_add_return_relaxed} & \tco{R*[ONCE] ->rmw W*[ONCE]} \\
	\tco{atomic_fetch_add_relaxed} & \\
	\tco{atomic_fetch_and_relaxed} & \\
	\tco{atomic_xchg_relaxed} & \\
	\tco{xchg_relaxed} & \\
	\tco{atomic_add_negative_relaxed} & \\ \Extraspace
\Dark	\tco{atomic_add_return_acquire} & \tco{R*[ACQUIRE] ->rmw W*[ACQUIRE]} \\
\Dark	\tco{atomic_fetch_add_acquire} & \\
\Dark	\tco{atomic_fetch_and_acquire} & \\
\Dark	\tco{atomic_xchg_acquire} & \\
\Dark	\tco{xchg_acquire} & \\
\Dark	\tco{atomic_add_negative_acquire} & \\ \Extraspace
	\tco{atomic_add_return_release} & \tco{R*[RELEASE] ->rmw W*[RELEASE]} \\
	\tco{atomic_fetch_add_release} & \\
	\tco{atomic_fetch_and_release} & \\
	\tco{atomic_xchg_release} & \\
	\tco{xchg_release} & \\
	\tco{atomic_add_negative_release} & \\
	\midrule \Extraspace
	\multicolumn{2}{@{}l}{Conditional RMW ops} \\
	C macro  &  Events \\
	\cmidrule(r){1-1} \cmidrule(l){2-2}
\Dark	\tco{atomic_cmpxchg} & On success: \tco{R*[MB] ->rmw W*[MB]}  \\
\Dark                        & On failure: \tco{R*[MB]} \\
\Dark	\tco{cmpxchg} & \\
\Dark   \tco{atomic_add_unless} & \\ \Extraspace
	\tco{atomic_cmpxchg_relaxed} & On success: \tco{R*[ONCE] ->rmw W*[ONCE]} \\
	                             & On failure: \tco{R*[ONCE]} \\ \Extraspace
\Dark	\tco{atomic_cmpxchg_acquire} & On success: \tco{R*[ACQUIRE] ->rmw W*[ACQUIRE]} \\
\Dark	                             & On failure: \tco{R*[ACQUIRE]} \\ \Extraspace
	\tco{atomic_cmpxchg_release} & On success: \tco{R*[RELEASE] ->rmw W*[RELEASE]} \\
	                             & On failure: \tco{R*[RELEASE]} \\ \Extraspace
\Dark	\tco{spin_trylock}           & On success: \tco{LKR ->po LKW} \\
\Dark	                             & On failure: \tco{LF} \\
\end{longtable}
}
